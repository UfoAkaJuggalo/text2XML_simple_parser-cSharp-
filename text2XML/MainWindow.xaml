<Window x:Class="text2XML.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:encoding="UTF-8"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:text2XML"
        mc:Ignorable="d"
        Title="Text to XML" Height="511" Width="823">
    <Window.Effect>
        <DropShadowEffect/>
    </Window.Effect>
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Open" Executed="CommandBinding_Open"/>
        <CommandBinding Command="ApplicationCommands.Close" Executed="CommandBinding_Exit"/>
    </Window.CommandBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Grid.Row="0" HorizontalContentAlignment="Right" Margin="0,10,0,0" FontSize="14" FontWeight="Bold">Source Text</Label>
            <Label Grid.Column="1" Grid.Row="0" HorizontalContentAlignment="Right" FontSize="14" FontWeight="Bold" Margin="0,10,25,0">XML</Label>
            <TextBox x:Name="txtBoxSource" Grid.Column="0" Grid.Row="1" AcceptsTab="True" AcceptsReturn="True" Margin="25,10,10,10" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
            <TextBox x:Name="txtBoxXML" Grid.Column="1" Grid.Row="1" AcceptsTab="True" AcceptsReturn="True" Margin="10,10,25,10" IsReadOnly="True" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"/>
        </Grid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Margin="10,0" Padding="5" Command="ApplicationCommands.Open">Open Plain Text</Button>
            <Button Margin="10,0" Padding="5">Parse text to XML</Button>
            <Button Margin="10,0" Padding="5">Save XML</Button>
        </StackPanel>
        <DockPanel Grid.Row="0">
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="_File">
                    <MenuItem Header="_Open Text File" Command="ApplicationCommands.Open"/>
                    <MenuItem Header="_Save XML File"/>
                    <Separator/>
                    <MenuItem Header="_Exit" Command="ApplicationCommands.Close"/>
                </MenuItem>
                <MenuItem Header="_Parse text to XML"/>
                <MenuItem Header="_About"/>
            </Menu>
        </DockPanel>
    </Grid>
</Window>
